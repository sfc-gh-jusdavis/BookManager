# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
env/
*.egg-info/
dist/
build/

# Node
node_modules/
node_modules_darwin_backup/
.next/
dist/
.vite/
*.tsbuildinfo

# Environment / Secrets
.env
.env.local
.env.*.local
backend/.env
*.pem
*.key
connections.toml

# Docker
docker-compose.override.yml

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# OS
Thumbs.db

# Coverage / Testing
htmlcov/
.coverage
.pytest_cache/
coverage/

# Cortex Code local state (plans are stored per-session, not committed)
.snowflake/
# .cortex/ is intentionally NOT excluded — project commands and context rules are committed

# Plan files at root (keep in docs/plans/ instead)
*.plan.md
